import { FourierResult } from "./Fourier";

export default class FundamentalFrequency {
    static calculateFundamentalAlgo1(result: FourierResult) {
        // algo1: find frequency with max magnitude and return
    }

    static calculateFundamentalAlgo2(result: FourierResult) {
        // algo2: find top K frequencies by magnitude
        // find the spacing (∆frequency) between top K frequencies
        // return the most common ∆f or the lowest frequency with that spacing
        // TODO:
    }
}